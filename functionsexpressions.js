/* Function expressions is another way, syntax, to define functions. Functions are objetcs too. */

const square = function (num) {
    return num * num;
}

// square(7); 49

function add(x, y) {
    return x + y;
}

const sum = function (x, y) { //anonymous function expression
    return x + y; 
}

const suma = function inventario(x,y) { //named function expression
    return x + y;
}

// The method console.dir() displays an interactive list of the properties of the specified JavaScript object.